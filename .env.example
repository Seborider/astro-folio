# ---------------------------------------------------------------
# Sanity (optional). When set, pages read from the CMS instead of
# the local JSON content collection. Leave unset to use JSON files.
# Find the project id at https://www.sanity.io/manage
# ---------------------------------------------------------------
PUBLIC_SANITY_PROJECT_ID=
PUBLIC_SANITY_DATASET=production
